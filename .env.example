# Optional. Leave unset to run entirely on the built-in local persistence layer.
# Only the public anon key belongs here -- never a service-role key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Azure OpenAI (GPT-5). Client-side only for now -- this key ships in the
# browser bundle and is visible in network requests. Fine for an internal/demo
# deployment behind auth; move it server-side before any public release.

